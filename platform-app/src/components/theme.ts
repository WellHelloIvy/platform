import {createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    // type: 'light',
    primary: {
      main: '#122ab3',
    },
    secondary: {
      main: '#ffd600',
      dark: '#ffa000',
    },
  },
});

export default theme;
